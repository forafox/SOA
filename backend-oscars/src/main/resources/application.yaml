spring:
  application:
    name: backend-oscars
  web:
    resources:
      static-locations: classpath:/static/
      cache:
        cachecontrol:
          max-age: 3600
server:
  port: 8080
  servlet:
    context-path: /
movies:
  api:
    base-url: http://localhost:8081/api